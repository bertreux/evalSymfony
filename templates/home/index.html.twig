{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <p>Accueil</p>
    {{ form_start(form) }}
    <div class="row">
        <div class="col-3 champFormulaire">
            {{ form_row(form.date_heur_depart) }}
        </div>
        <div class="col-3 champFormulaire">
            {{ form_row(form.date_heur_fin) }}
        </div>
        <div class="col-3 champFormulaire">
            {{ form_row(form.submit) }}
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}
